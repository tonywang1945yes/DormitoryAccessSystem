项目分工：
1个人负责写界面，界面写在ui包下，界面与逻辑层的接口定义在service包里。-->姜昱华
1个人负责写调度逻辑，内容写在bl包下，负责实现service包中定义的接口，可以调用util包和dao包中的方法。 -->张雨奇
1个人负责写发邮件的模块，封装好所有静态方法，写在util包的mail子包下。 -->孙逸伦
1个人负责写读取excel数据的模块，封装好所有静态方法，写在util包的excel子包下。 -->冯柯翔
1个人负责写用jdbc读取mysql中的数据,将读出的数据封装成entity包中的对象并返回，写在dao包中。（如果可以的话也可以直接读取sqlServer2008中的数据）-->黄国钊

最新修改：王瑞华 2018.12.14 12:15:00